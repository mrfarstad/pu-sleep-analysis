version: '2'

services:
  db:
    image: mysql
    environment:
      MYSQL_ROOT_PASSWORD: gruppe16
      MYSQL_DATABASE: gruppe16
      MYSQL_USER: gruppe16
      MYSQL_PASSWORD: gruppe16
    ports:
      - 3306:3306

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
